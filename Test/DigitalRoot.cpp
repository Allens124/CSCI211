#include <iostream>
using namespace std;

string digitalRoot(string n)
{
    if (n.size() == 1)
    {
        return n;
    }
    int resultInt = 0;
    for (int i = 0; i < n.size(); i++)
    {
        resultInt += s[i]-'0';
    }
}

int main()
{
    //code;
    return 0;
}